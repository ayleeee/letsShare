<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/home.css">
    <title>Document</title>
</head>
<body>
    <h1> 共有したいファイルを選択してください</h1>
    <div id='main'>
    <form action="/" method="post" enctype="multipart/form-data">
        <input type="file" name='myfile' id="file" class="inputfile" required>
        <label for="file" id="fileLabel"> ファイル選択 </label>
    </div>

    <div id='btm'>
        <button id='btn'>アップロード</button>
    </div>
</form>

</body>
<script>

    document.getElementById('file').onchange=()=>{
        const filename=document.getElementById('fileLabel');
        filename.innerHTML=file.value.replace(/^.*[\\\/]/, '');
    }

    const btn = document.getElementById('btn');
    btn.addEventListener('click',(e)=>{
        if(document.getElementById('file').value==""){
            alert("ファイルを選択してください");
            return false;
        }
    })

</script>
</html>
